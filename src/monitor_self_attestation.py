"""Self-attestation for Task 2. Edit the two values below; see README.md.

By setting the flag to True, you attest that you have:

- filled in the FedWatch math (Task 1) so the full pipeline runs,
- pushed the completed pipeline to a separate PUBLIC repo under your
  personal GitHub account (this assignment repo stays private),
- added your DATABENTO_API_KEY there as an Actions repository secret
  (Settings -> Secrets and variables -> Actions), never committed in code,
- run the "Build and Deploy FedWatch Site" workflow successfully, and
- enabled GitHub Pages on the gh-pages branch, so the chartbook site --
  including the next-FOMC-meeting forecast chart -- is live and rebuilds
  itself every morning on the cron schedule.

Paste the URL of the live site, e.g. "https://jdoe.github.io/finm32900-hw4-fedwatch/".
"""

I_HAVE_PUBLISHED_MY_FEDWATCH_MONITOR_SITE = True
URL_TO_MY_FEDWATCH_MONITOR_SITE = "https://rileykhaas.github.io/finm32900-hw4-fedwatch/"
